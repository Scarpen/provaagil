<%= simple_form_for(@papel) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
    <%= f.input :descricao %>
  </div>

  <div class="form-actions">
     <%= f.button :submit, "Enviar", class: "btn btn-success" %>
  </div>
<% end %>
